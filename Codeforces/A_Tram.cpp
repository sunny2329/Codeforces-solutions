#include <cmath>
#include <iostream>
#include <vector>
using namespace std;

int main()
{
    int n;
    cin >> n;
    vector<int> x,y;
    while(n--){
        int a,b;
        cin >> a >> b;
        x.push_back(a);
        y.push_back(b);
    }
    
   
}